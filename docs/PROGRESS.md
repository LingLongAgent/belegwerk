# Belegwerk — Progress

Neueste oben.

## Done

- **M5 · Angebot-Formular** — neues `Document.valid_until` (DateField, optional) für die Angebots-Gültigkeit; `build_offer()` → py_doc `Offer` (Sender/Empfänger/Meta/Positionen wie bei der Rechnung, `valid_until` als deutsches Datum bzw. leer), `render_pdf()`-Dispatch um Typ Angebot = Form.A erweitert. Formulare: `OfferForm` (gleiche Struktur wie `InvoiceForm`, aber `valid_until` statt Zahlungsbedingungen); Sender-Scoping in `SenderScopedFormMixin` und Empfänger-Widgets in `RECIPIENT_WIDGETS` ausgelagert (von Invoice + Offer geteilt); Positionen über `OfferItemFormSet` (= dasselbe Inline-Formset wie die Rechnung). View `offer_create` spiegelt `invoice_create` (Datum=heute, Standard-Absender, `?recipient=<id>`-Vorbelegung; POST speichert Document(Typ=Angebot)+Positionen). Template `offer_form.html` mit Live-Summe; Typ-Auswahl verlinkt „Angebot". PDF-Test mit echtem py_doc + pypdf (Empfänger/Position/Betreff/Gültig-bis-Datum im Text). 11 neue Tests, gesamt **56 grün**, ruff sauber.
- **M4 · Rechnung-Formular + Positionen** — `DocumentItem`-Modell (Beschreibung/Menge/Einheit/Einzelpreis in Cent/USt-Satz als Bruch), `to_line_item()` → py_doc `InvoiceItem`. `Document`: `payment_terms`-Feld, `to_line_items()`, `build_invoice()` → py_doc `Invoice`, `render_pdf()` (Dispatch nach Typ; Invoice = Form.A, andere Typen folgen). Formulare: `InvoiceForm` (Sender auf Nutzer gescopt, Standardprofil vorausgewählt; Empfänger inline) + `DocumentItemForm` (Eingabe in €, Speicherung in Cent; USt aus 19/7/0 %) als Inline-Formset. View `invoice_create`: GET füllt Datum=heute, Standard-Absender, Empfänger via `?recipient=<id>` vor; POST speichert Document(Typ=Rechnung)+Positionen in Reihenfolge. Template mit Abschnitten, Positions-Tabelle, „+ Zeile"/Entfernen und Live-Summe (Netto/Brutto) per JS; Typ-Auswahl verlinkt „Rechnung". Admin-Inline. PDF-Test mit echtem py_doc + pypdf (Empfänger/Position/Betreff im Text). 12 neue Tests, gesamt **45 grün**, ruff sauber.
- **M3 · Empfänger/Kunden (Adressbuch)** — `Recipient`-Modell (gleiche Adressfelder wie `Document.recipient_*`, pro Nutzer), `to_recipient()` → py_doc `Party` (Blanks→None), `as_document_initial()` liefert `recipient_*`-Initialwerte für die späteren Dokument-Formulare (M4+). Formular mit Platzhaltern, List/Create/Edit/Delete (ownership-scoped), Sidebar-Link „Empfänger", Admin. 12 neue Tests (Mapping, Initial-Keys, str-Regel, View-Scoping/CRUD). Gesamt 33 grün, ruff sauber.
- **M2 · Dokument-Datenmodell** — `Document`-Modell (Typ-Choices Rechnung/Angebot/Vertrag/Zahlungserinnerung, Inline-Empfänger, Nummer/Datum/Betreff, Absender-FK mit PROTECT, pro Nutzer). `to_recipient()` → py_doc `Party`, `to_meta()` → `DocumentMeta` (deutsches Datum, typ-abhängiges Nummern-Label im Informationsblock). Admin-Registrierung. 7 Tests grün (Mapping, Blanks→None, Label-pro-Typ, PROTECT). Gesamt 21 grün.
- **M1 · Absender-Profil** — `SenderProfile`-Modell (Party+Sender-Felder, `to_sender()` → py_doc), pro Nutzer, Standardprofil-Regel; Formular mit Abschnitten/Platzhaltern, List/Create/Edit/Delete (ownership-scoped), Sidebar-Link, Admin. 14 Tests grün (Modell-Mapping, Default-Regel, View-Scoping).
- **M0 · Scaffold** — Django + Design + Auth + Typ-Auswahl + py_doc-Engine. Tests grün.
